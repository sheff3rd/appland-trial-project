<%= form_with(model: appmap) do |form| %>
  <% if appmap.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(appmap.errors.count, "error") %> prohibited this appmap from being saved:</h2>

      <ul>
        <% appmap.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :path, style: "display: block" %>
    <%= form.text_field :path %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
